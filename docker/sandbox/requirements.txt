# Python deps for the office-doc skills (M10 step 6) — docx/pdf/pptx/xlsx.
# Unpinned, matching docker/mcp-drive/requirements.txt: these back the vendored
# anthropics/skills scripts, not a tool-name contract, so latest-compatible is fine.

# pdf skill: text/table extract, page ops, rasterize (pdf2image needs poppler-utils),
# and PDF authoring.
pdfplumber
pypdf
pdf2image
reportlab
Pillow

# xlsx skill: read/write workbooks, formulas, formatting.
openpyxl

# docx/pptx/xlsx skills: raw OOXML unpack-edit-repack (the office/ helper modules).
lxml
defusedxml

# pptx skill: content read-back. The [pptx] extra pulls python-pptx.
markitdown[pptx]
