# Installing screening

1. Copy `skills/screening/SKILL.md` from this package into the core skills
   directory as `skills/screening/SKILL.md` using `self_edit`.
2. That is all — screening is prompt policy, not code. Read the skill once
   after installing so its rules are fresh in this session.
