"""Google Calendar integration (M5): the ``mcp-calendar`` container's tool catalog.

The shared OAuth minter lives in :mod:`chief.tools.google.auth` (one token covers
calendar + drive + sheets); this package is now just the calendar tool catalog.
"""
