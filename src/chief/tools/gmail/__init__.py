"""Gmail integration (M8): the ``mcp-gmail`` container's tool catalog.

The shared OAuth minter lives in :mod:`chief.tools.google.auth` (one token covers
calendar + drive + sheets + gmail); this package is just the Gmail tool catalog.
"""
